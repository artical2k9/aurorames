package com.mes.routing.route.domain;

/** Fixed labour activity types for standard times (FR-014). Not user-extensible. */
public enum LabourActivityType {
    SETUP,
    RUN,
    INSPECTION,
    TRANSPORT
}
